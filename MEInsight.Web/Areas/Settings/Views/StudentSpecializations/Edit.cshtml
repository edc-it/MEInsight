@model MEInsight.Entities.Reference.RefStudentSpecialization

@{
    ViewData["Title"] = @Localizer["Edit"].Value + " " + @Localizer["Student Specialization"].Value;
}

<h2>@Localizer["Edit"]</h2>

<h4>@Localizer["Student Specialization"]</h4>
<hr />
<div class="row">
    <div class="col-md-6 col-sm-8">
        <!--form start-->
        <form asp-action="Edit" id="main-form">
            <div asp-validation-summary="All" class="text-danger"></div>
            <input type="hidden" name="ParentId" value="@ViewBag.ParentId" />
            <input type="hidden" asp-for="RefStudentSpecializationId" />
            
            <div class="form-group" asp-enabled="StudentSpecializationCode">
                <!--input start-->
                <label asp-for="StudentSpecializationCode" class="control-label"></label>
                <input asp-for="StudentSpecializationCode" autocomplete="off" class="form-control" />
                <span asp-validation-for="StudentSpecializationCode" class="text-danger"></span>
                <!--input end-->
            </div>
            
            
            <div class="form-group" asp-enabled="StudentSpecialization">
                <!--input start-->
                <label asp-for="StudentSpecialization" class="control-label"></label>
                <input asp-for="StudentSpecialization" autocomplete="off" class="form-control" />
                <span asp-validation-for="StudentSpecialization" class="text-danger"></span>
                <!--input end-->
            </div>
            
            <!-- button group start -->
            <div class="form-group text-end mt-5">
                <a id="cancelbutton" class="btn btn-secondary me-2" asp-action="Index" asp-route-id="@ViewBag.ParentId">
                    <i class="fas fa-times"></i> @Localizer["Cancel"]
                </a>
                <button id="submitbutton" type="submit" value="Save" class="btn btn-primary ladda-button" data-style="expand-left" data-spinner-color="white">
                    <span class="ladda-label">
                        <i class="fas fa-check"></i> @Localizer["Save"]
                    </span>
                </button>
            </div>
            <!-- button group end -->
        </form>
        <!--form end-->
    </div>
</div>

@section Styles {
    @{await Html.RenderPartialAsync("_EditStylesPartial");}
}
@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
    @{await Html.RenderPartialAsync("_EditScriptsPartial");}
}
