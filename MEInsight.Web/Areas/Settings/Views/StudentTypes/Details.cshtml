@model MEInsight.Entities.Reference.RefStudentType

@{
    ViewData["Title"] = @Localizer["Student Type"].Value + " " + @Localizer["Details"].Value;
}

<h2>@Localizer["Details"]</h2>

<div>
    <h4>@Localizer["Student Type"]</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.StudentTypeCode)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.StudentTypeCode)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.StudentType)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.StudentType)
        </dd>
    </dl>

    <hr />
    <dl class="row">
    </dl>


</div>
<div>
        <!--footer buttons start-->
        <div class="row mt-5">
            <div class="col">
                <a asp-action="Index" asp-route-id="@ViewBag.ParentId" class="btn btn-secondary me-1"><i class="fas fa-chevron-left"></i> @Localizer["Back to List"]</a>
                <a policy="RequireEditRole" asp-action="Edit" asp-route-id="@Model.RefStudentTypeId" class="btn btn-primary"><i class="fas fa-pencil-alt"></i> @Localizer["Edit"]</a>
            </div>
            <div class="col text-end"></div>
        </div>
        <!--footer buttons end-->
</div>
